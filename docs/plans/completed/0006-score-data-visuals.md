# 0006: 직관적 점수, 사용자 CSV 분석, 포인트 클라우드 표시

- 상태: completed
- 시작일: 2026-09-19

## 완료 기준

- [x] UI 점수/미터/설정을 100=기준에 가까움, 0=큰 변화로 통일. 기존 판정/CSV 규칙 값 유지
- [x] database/temp 원본은 읽기만 하고 pandas 분석 결과를 로컬 제외 경로에 저장
- [x] 기존 Pose 33점 중 가시성이 충분한 점·연결을 표시, 선택 가능한 파티클 조립 효과
- [x] 장식용 점과 측정 키포인트 구분, 낮은 품질/동작 줄이기/끄기 지원
- [x] make check 및 합성 브라우저 검증 결과 보고. 루트 AGENTS.md 존재/실행 경로 확인

## 범위

얼굴 전용 모델·턱 추적, 자동 임계값 튜닝/학습, 실제 CSV 원본 변경·커밋은 포함하지 않는다.
사용자 기록 한 파일로 정상 움직임 분포와 수집 품질을 확인하며 모델 정확도라고 해석하지 않는다.

## 검증 기록

- pandas 2.2.3로 사용자 CSV 328행을 분석하고 시간축 그래프/보고서를 database/temp/analysis에 저장했다. 원본 SHA-256 전후 일치를 확인했다.
- 실제 파일과 개인 분석 출력은 Git 제외했고, 강제 추가 시에도 하네스가 금지 경로를 검출하도록 보강했다.
- make check: 구조/링크/산출물 검사, Python 10개·프론트 21개, 타입·빌드 최종 재실행 통과.
- 점수 반전 후에도 동일 경계에서 알림이 발생하는지 합성 테스트했다. 시각 효과는 최대 445개 파티클이며 입력을 변형하지 않는지 확인했다.
- 브라우저: 합성 기준 100.0점, 큰 변화 0.0점, 3초 후 이벤트 1건을 확인했다. 설정 알림 기준 30점 표시를 확인했다.
- 매트릭스 재조립 스크린샷, 동작 줄이기 시 재조립 비활성화, 키포인트 숨김, 품질 유실 시 효과 제거를 확인했다. 실제 사람 추가 촬영 없이 QA했고 임시 진입점은 삭제했다.
- 실제 사람의 효과 활성화 전후 FPS/정확도는 미검증이다. 사용자 파일에 정상 라벨만 있어 분류 성능은 평가하지 않았다.
- 원격 CI/푸시는 실행하지 않았다. 검증은 로컬 make check이다.

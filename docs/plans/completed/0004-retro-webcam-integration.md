# 0004: 기존 PoseGood 프론트에 웹캠 프로토타입 통합

- 상태: completed
- 기반: origin/feature/retro-redesign (60ec460)
- 작업 브랜치: codex/retro-webcam

## 범위

사용자가 기존 프론트 feature 브랜치에 기능을 붙이도록 정정했다.
기존 PoseGood 디자인·6개 화면·React 18·복귀 유지시간 정책을 유지하면서 웹캠과 개인 기준 규칙을 통합한다.
하네스 문서는 복구하며, 앞서 만든 독립 화면은 Git stash와 로컬 임시 백업으로 보존한다.
원격 푸시·배포는 범위에 포함하지 않는다.

빌드 도구는 기존 버전의 audit 결과를 반영해 호환되는 Vite 8·Vitest 5로 갱신했다.

## 완료 기준

- [x] 기존 UI의 준비 → 기준 등록 → 실시간 측정에 실제 카메라 연결
- [x] 시연 모드 유지, 웹캠에서 확률·각도·학습 모델을 오인하지 않는 표시
- [x] 휴식·입력 유실·재알림·복귀·화면 이동 시 카메라 정리 검증
- [x] 기존/신규 테스트, 타입·빌드·하네스 검증과 브라우저 확인
- [x] 사용 안내와 통합 결정 기록

## 보존 기록

- 이전 하네스·독립 프로토타입 stash: `fd28faa772b9314caa6d864ca1dcb8a59f40b16e`
- 별도 로컬 백업: `/private/tmp/posture-before-retro-g5cvdcbk`
- 대상 feature 브랜치에는 아직 커밋·원격 푸시하지 않았다.

## 검증 기록

- `make check`: Python 10개, 프론트 9개 테스트와 타입·프로덕션 빌드 통과.
- 브라우저 검증은 합성 영상/랜드마크로 진행한다. 실제 사람 자세의 정확도·조명·장치별 성능은 미검증이다.
- 브라우저: 5초 기준 등록, 실시간 변화 점수, 3초 후 이벤트/알림, 휴식 중 이벤트 중단, 입력 유실의 시간 제외, 종료 통계를 합성 랜드마크로 확인했다.
- 실제 MediaPipe WASM/모델 초기화 후 사람 없는 합성 영상이 품질 불충족으로 처리되어 등록·측정 버튼이 비활성화됨을 확인했다.
- 종료 및 홈 이동 후 합성 MediaStream 트랙이 ended로 바뀜을 확인했다.
- 기존 발표용 보정/시연 배속/다음 구간 흐름, 합성 권한 거부 시 재연결 안내와 등록 차단을 확인했다. 임시 QA 진입점은 제거했다.

# PoseGood 디자인 시스템

레트로 에디토리얼 피치덱 톤 (참고: Figma Community "Agency Pitch"). 발표용 프로젝터 환경을 기준으로 밝은 종이 바탕 위에 검정 패널을 둔다. 토큰의 원본은 `frontend/src/styles.css`의 `:root`다.

## 색

| 역할 | 토큰 | 값 | 용도 |
|---|---|---|---|
| 바탕 | `--paper` | `#e6e1d5` | 페이지 바탕 |
| 시트 | `--paper-2` | `#efebe2` | 카드, 입력칸 |
| 눌린 종이 | `--paper-3` | `#d6d0c1` | hover, 미터 트랙, 바인더 링 |
| 잉크 | `--ink` | `#151412` | 본문, 굵은 괘선, 막대 차트 |
| 보조 잉크 | `--ink-2` / `--muted` | `#4a463f` / `#625d54` | 설명, 라벨 |
| 패널 | `--panel` | `#141312` | 사이드바, 카메라 화면, 강조 카드(`.card.dark`) |
| 패널 글자 | `--panel-text` / `--panel-muted` | `#ede8dc` / `#a39d90` | 검정 패널 위 글자 |
| 행동 색 | `--accent` | `#e2683c` | 주요 버튼, 현재 메뉴, 임계값 초과, 차트 선. 이 외에는 쓰지 않는다 |

상태색은 한 상태마다 세 단계를 둔다. 종이 위 글자는 기본값, 막대·채움은 `-fill`, 검정 패널 위 글자는 `-hi`.

| 상태 | 글자 | 채움 | 패널 위 |
|---|---|---|---|
| 바른 자세 `good` | `#2e6b60` | `#3e8b7c` | `#6fc2af` |
| 자세 붕괴 `bad` | `#a3314d` | `#c2456a` | `#ee6a86` |
| 확인 중 `warn` | `#7e5a1c` | `#c9973f` | `#e3b25a` |
| 판정 불가 `unknown` | `#5f5b53` | `#8f8a7f` | `#a39d90` |

## 글꼴 (모두 npm으로 프로젝트에 내장)

| 토큰 | 글꼴 | 용도 |
|---|---|---|
| `--font-body` | Pretendard Variable | 본문, 버튼, 라벨, 표 |
| `--font-title` | Black Han Sans | 페이지 제목, 판정 단어, 빈 화면 제목 |
| `--font-figure` | Anton | 큰 숫자(지표, 확률, 유지율), 워드마크 |
| `--font-stencil` | Stardos Stencil | 측정 준비 단계 번호 |

버튼·라벨·표 숫자에는 Anton과 Black Han Sans를 쓰지 않는다.

## 모양과 구조
- 모서리: 조작 요소 4px(`--radius-sm`), 카메라 화면 6px(`--radius`). 둥근 알약 모양은 쓰지 않는다.
- 카드(`.card`)는 위쪽 3px 잉크 괘선이 있는 시트다. 검정 강조 카드(`.card.dark`)는 위쪽 괘선이 오렌지다. 카드 안에 카드를 넣지 않는다.
- 요약 지표는 카드 대신 `.ledger` 가로 장부 줄로 놓고, 칸 사이를 1px 괘선으로 나눈다.
- 페이지 머리는 3px 잉크 괘선으로 닫는다.
- 바인더 링(`<Rings />`)은 사이드바와 본문 경계, 로그인 표지 왼쪽에만 쓴다.
- 아이콘은 `@phosphor-icons/react` 한 가지만 쓰고, 굵기는 `bold` 또는 `fill`이다. 유니코드 기호로 아이콘을 대신하지 않는다.

## 모션
- 곡선: `--ease-out` `cubic-bezier(0.23, 1, 0.32, 1)`.
- 누르기: 버튼·칩 `scale(0.97)`, 160ms.
- 등장: 경고·확인 막대·이벤트 행 `rise` 220–240ms, 토스트 240ms.
- 대표 동작: 판정이 바뀌면 판정 단어가 `clip-path` 와이프로 바뀐다(280ms).
- `prefers-reduced-motion`에서는 모든 모션을 거의 0으로 줄인다.
- hover 효과는 `(hover: hover) and (pointer: fine)` 안에서만 적용한다.

## 반응형
- 1180px 이하: 측정 화면 1열, 지표 장부 2열, 로그인 표지 1열.
- 860px 이하: 사이드바가 위로 올라가고, 모든 그리드가 1열, 바인더 링은 숨긴다.

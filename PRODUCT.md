# PoseGood

## Product
웹캠 기반 착석 자세 분석 및 교정 보조 시스템. MediaPipe Pose로 관절 좌표를 뽑고 LSTM이 자세 붕괴 확률을 내며, 규칙 기반 상태 기계가 붕괴 이벤트를 확정·기록한다. 착석 시간, 자세 붕괴 주기, 붕괴 지속 시간, 회복 시간을 통계로 보여 주고 기간별 개선도를 비교한다.

## Audience
장시간 컴퓨터 앞에 앉는 사용자(재택근무자, 학생, 게이머). 현재 단계의 1차 청중은 기업연계 프로젝트 발표의 심사위원이다.

## Current surface
`frontend/`는 발표용 UI 데모다. 카메라와 모델은 연결되지 않았고, 미리 정한 시나리오를 재생하면서 화면과 판정 규칙만 실제로 동작한다. 백엔드(Spring Boot), DB(MySQL), MediaPipe 연동, LSTM 학습·추론은 아직 없다.

## Truths that must not change
- 판정 규칙: 붕괴 확률 임계값 이상이 확정 지속 시간만큼 이어질 때만 이벤트로 확정, 재알림 간격 억제, 정상 복귀 유지 시간.
- 통계 정의: 유효 측정 시간 = 전체 − 일시정지 − 판정 불가. 유지율은 유효 측정 시간 기준. 유효 측정 시간이 0이면 "계산 불가".
- 시연용 데이터는 가짜이며 화면에 시연용임을 밝힌다.

## Brand commitments
- 시각 톤: Figma Community "Agency Pitch" 슬라이드 템플릿의 레트로 에디토리얼 톤 (사용자 지정).
- 발표 환경: 프로젝터. 밝은 종이 바탕 + 검정 패널.
- 폰트는 프로젝트에 내장한다 (오프라인에서도 동일하게 보여야 함).
- 배포: develop → main PR 후 GitHub에서 배포하고, 발표 때는 배포 주소로 접속한다.
